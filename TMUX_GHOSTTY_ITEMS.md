# TODO

## tmux statusline improvements
- 'Ctrl + b' 같은 prefix 조합키를 이용해서 tmux를 조작하는데, 해당 prefix가 입력된 상태일 때 statusline에서 statusline의 색 전체를 변경하거나 혹은 mode를 보여주는 라벨을 만들어서 색을 변경해서 보여주어야 한다
- Sync 상태 표시
- 기본적인 cheat hint 표시

## tmux keybinding
- 'Ctrl+b' 대신 'Ctrl+a'를 prefix key로 변경
