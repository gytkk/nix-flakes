# Prefix.
set-option -g prefix C-a
set-option -g prefix2 None
unbind-key C-b
bind-key -N "Send prefix to nested tmux sessions" C-a send-prefix

# Window management.
bind-key -N "Kill current window" X kill-window
